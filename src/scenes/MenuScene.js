import Phaser from 'phaser';

export default class MenuScene extends Phaser.Scene {
    constructor() {
        super({ key: 'MenuScene' });
    }

    preload() {
        // Carregar imagens
    }

    create() {
        // Adicionar elementos de cena como sprites        
    }

    update() {
        // Atualizar lógica se necessário
    }
}
